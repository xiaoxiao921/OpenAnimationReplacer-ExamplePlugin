# SKSE64 Plugin Example

### Setup
```bat
xmake update -s dev

xmake project -k compile_commands --lsp=clangd
```

### Build
```bat
xmake update -s dev

xmake project -k compile_commands --lsp=clangd

xmake build
```